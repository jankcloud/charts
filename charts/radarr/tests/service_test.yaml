suite: Service
templates:
  - templates/service.yaml
tests:

  - it: renders a Service for Radarr
    asserts:
      - isKind:
          of: Service
      - equal:
          path: metadata.name
          value: RELEASE-NAME-radarr

  - it: exposes Radarr on port 7878 named http
    asserts:
      - contains:
          path: spec.ports
          content:
            name: http
            port: 7878
            targetPort: http
            protocol: TCP
