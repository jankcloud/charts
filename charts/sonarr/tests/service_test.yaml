suite: Service
templates:
  - templates/service.yaml
tests:

  - it: renders a Service for Sonarr
    asserts:
      - isKind:
          of: Service
      - equal:
          path: metadata.name
          value: RELEASE-NAME-sonarr

  - it: exposes Sonarr on port 8989 named http
    asserts:
      - contains:
          path: spec.ports
          content:
            name: http
            port: 8989
            targetPort: http
            protocol: TCP
